# Sprint-01 PRD 重写完成 — 按 LingoBridge 规范

**时间**: 2026-06-10 19:17  
**产出**: 2 个文件

```
prds/
├── json/
│   └── sprint01-prd-260610-v0.1.json    (12.4 KB)
└── md/
    └── sprint01-prd-260610-v0.1.md      (2.4 KB)
```

## 主要改进

删除旧的 4 个不规范文件，按 LingoBridge 格式重写为 **1 个 sprint 级 PRD**：

| 字段 | 旧版本 | 新版本 |
|---|---|---|
| 结构 | 数据模型定义 | sprint 交付物（objective + KR + tasks） |
| task 粒度 | 功能描述 | ownerAgentPrompt + targetFiles + steps + acceptance |
| repoReality | 无 | 14 条项目现状精确审计 |
| strategy | 无 | 优先级 + 执行顺序 |
| guardrails | 仅技术风险 | 包含业务底线（不破坏已用功能） |
| exitCriteria | 无 | 验收条件 |
| 模拟数据标注 | 无 | 所有 mock 数据标注 `is_simulated: true` |

## Sprint-01 范围

**系统后台监控面板**，覆盖 TaskMonitor + SystemStatus 两个视图的 mock→API 改造。7 个任务按序执行：

1. health_checker 引擎（并发 6 服务 ping，10s 缓存）
2. task_tracker + minio_stats（任务追踪 + 模拟生成器）
3. log_collector（Redis 环形缓冲日志）
4. FastAPI 路由（4 个新 router，约 15 个端点）
5. 前端 TaskMonitor 改造
6. 前端 SystemStatus 改造
7. Docker healthcheck + 清理 constants
